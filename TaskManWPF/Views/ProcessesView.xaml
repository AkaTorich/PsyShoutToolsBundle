<UserControl x:Class="TaskManWPF.Views.ProcessesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Процессы" Style="{StaticResource PageTitleStyle}"/>
            <TextBlock Style="{StaticResource SubtitleStyle}">
                <Run Text="Управление запущенными процессами • "/>
                <Run Text="{Binding ProcessCount, Mode=OneWay}" Foreground="{StaticResource PrimaryBrush}"/>
                <Run Text=" процессов"/>
            </TextBlock>
        </StackPanel>
        
        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MaxWidth="400"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0" 
                     Style="{StaticResource SearchBoxStyle}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Style="{StaticResource SecondaryButtonStyle}"
                        Content="🔄 Обновить"
                        Command="{Binding RefreshCommand}"
                        Margin="0,0,8,0"/>
                
                <Button Style="{StaticResource DangerButtonStyle}"
                        Content="⛔ Завершить"
                        Command="{Binding TerminateProcessCommand}"
                        IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                        Margin="0,0,8,0"/>
                
                <Button Style="{StaticResource SecondaryButtonStyle}"
                        Content="📁 Папка"
                        Command="{Binding OpenFileLocationCommand}"
                        IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"
                        Margin="0,0,8,0"/>
                
                <Button Style="{StaticResource SecondaryButtonStyle}"
                        Content="🔍 Google"
                        Command="{Binding SearchGoogleCommand}"
                        IsEnabled="{Binding SelectedProcess, Converter={StaticResource NullToBoolConverter}}"/>
            </StackPanel>
        </Grid>
        
        <!-- DataGrid -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <DataGrid Style="{StaticResource ModernDataGridStyle}"
                      ItemsSource="{Binding Processes}"
                      SelectedItem="{Binding SelectedProcess}"
                      ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeaderStyle}"
                      CellStyle="{StaticResource ModernDataGridCellStyle}"
                      RowStyle="{StaticResource ModernDataGridRowStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="180"/>
                    <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="70"/>
                    <DataGridTextColumn Header="Память (МБ)" Binding="{Binding MemoryMB, StringFormat=N0}" Width="100"/>
                    <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="200"/>
                    <DataGridTextColumn Header="Компания" Binding="{Binding Company}" Width="150"/>
                    <DataGridTextColumn Header="Путь" Binding="{Binding Path}" Width="*"/>
                </DataGrid.Columns>
                
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Завершить процесс" Command="{Binding TerminateProcessCommand}"/>
                        <MenuItem Header="Завершить дерево процессов" Command="{Binding TerminateProcessTreeCommand}"/>
                        <Separator/>
                        <MenuItem Header="Открыть расположение файла" Command="{Binding OpenFileLocationCommand}"/>
                        <MenuItem Header="Искать в Google" Command="{Binding SearchGoogleCommand}"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
            </DataGrid>
        </Border>
        
        <!-- Status Bar -->
        <Border Grid.Row="3" 
                Background="{StaticResource SurfaceBrush}" 
                CornerRadius="6" 
                Padding="16,10" 
                Margin="0,16,0,0">
            <TextBlock Text="{Binding StatusText}" 
                       Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="12"/>
        </Border>
    </Grid>
</UserControl>

