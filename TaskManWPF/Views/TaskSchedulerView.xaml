<UserControl x:Class="TaskManWPF.Views.TaskSchedulerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Планировщик задач" Style="{StaticResource PageTitleStyle}"/>
            <TextBlock Style="{StaticResource SubtitleStyle}">
                <Run Text="Управление запланированными задачами • "/>
                <Run Text="{Binding TaskCount, Mode=OneWay}" Foreground="{StaticResource PrimaryBrush}"/>
                <Run Text=" задач"/>
            </TextBlock>
        </StackPanel>
        
        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MaxWidth="400"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0" 
                     Style="{StaticResource SearchBoxStyle}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Style="{StaticResource SecondaryButtonStyle}"
                        Content="🔄 Обновить"
                        Command="{Binding RefreshCommand}"
                        Margin="0,0,8,0"/>
                
                <Button Style="{StaticResource PrimaryButtonStyle}"
                        Content="▶️ Запустить"
                        Command="{Binding RunTaskCommand}"
                        IsEnabled="{Binding SelectedTask, Converter={StaticResource NullToBoolConverter}}"
                        Margin="0,0,8,0"/>
                
                <Button Style="{StaticResource SecondaryButtonStyle}"
                        Content="⏹️ Остановить"
                        Command="{Binding StopTaskCommand}"
                        IsEnabled="{Binding SelectedTask, Converter={StaticResource NullToBoolConverter}}"
                        Margin="0,0,8,0"/>
                
                <Button Style="{StaticResource SecondaryButtonStyle}"
                        Content="✅ Включить"
                        Command="{Binding EnableTaskCommand}"
                        IsEnabled="{Binding SelectedTask, Converter={StaticResource NullToBoolConverter}}"
                        Margin="0,0,8,0"/>
                
                <Button Style="{StaticResource SecondaryButtonStyle}"
                        Content="⏸️ Отключить"
                        Command="{Binding DisableTaskCommand}"
                        IsEnabled="{Binding SelectedTask, Converter={StaticResource NullToBoolConverter}}"
                        Margin="0,0,8,0"/>
                
                <Button Style="{StaticResource DangerButtonStyle}"
                        Content="🗑️ Удалить"
                        Command="{Binding DeleteTaskCommand}"
                        IsEnabled="{Binding SelectedTask, Converter={StaticResource NullToBoolConverter}}"/>
            </StackPanel>
        </Grid>
        
        <!-- DataGrid -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <DataGrid Style="{StaticResource ModernDataGridStyle}"
                      ItemsSource="{Binding Tasks}"
                      SelectedItem="{Binding SelectedTask}"
                      ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeaderStyle}"
                      CellStyle="{StaticResource ModernDataGridCellStyle}"
                      RowStyle="{StaticResource ModernDataGridRowStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="200"/>
                    <DataGridTextColumn Header="Состояние" Binding="{Binding StateDisplay}" Width="100"/>
                    <DataGridTextColumn Header="Триггеры" Binding="{Binding Triggers}" Width="150"/>
                    <DataGridTextColumn Header="Последний запуск" Binding="{Binding LastRunDisplay}" Width="140"/>
                    <DataGridTextColumn Header="Следующий запуск" Binding="{Binding NextRunDisplay}" Width="140"/>
                    <DataGridTextColumn Header="Автор" Binding="{Binding Author}" Width="150"/>
                    <DataGridTextColumn Header="Путь" Binding="{Binding Path}" Width="*"/>
                </DataGrid.Columns>
                
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Запустить" Command="{Binding RunTaskCommand}"/>
                        <MenuItem Header="Остановить" Command="{Binding StopTaskCommand}"/>
                        <Separator/>
                        <MenuItem Header="Включить" Command="{Binding EnableTaskCommand}"/>
                        <MenuItem Header="Отключить" Command="{Binding DisableTaskCommand}"/>
                        <Separator/>
                        <MenuItem Header="Удалить" Command="{Binding DeleteTaskCommand}"/>
                        <Separator/>
                        <MenuItem Header="Свойства" Command="{Binding ShowPropertiesCommand}"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
            </DataGrid>
        </Border>
        
        <!-- Status Bar -->
        <Border Grid.Row="3" 
                Background="{StaticResource SurfaceBrush}" 
                CornerRadius="6" 
                Padding="16,10" 
                Margin="0,16,0,0">
            <TextBlock Text="{Binding StatusText}" 
                       Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="12"/>
        </Border>
    </Grid>
</UserControl>

