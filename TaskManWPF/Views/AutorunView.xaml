<UserControl x:Class="TaskManWPF.Views.AutorunView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Автозапуск" Style="{StaticResource PageTitleStyle}"/>
            <TextBlock Style="{StaticResource SubtitleStyle}">
                <Run Text="Управление программами автозагрузки • "/>
                <Run Text="{Binding AutorunCount, Mode=OneWay}" Foreground="{StaticResource PrimaryBrush}"/>
                <Run Text=" записей"/>
            </TextBlock>
        </StackPanel>
        
        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MaxWidth="400"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0" 
                     Style="{StaticResource SearchBoxStyle}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Style="{StaticResource SecondaryButtonStyle}"
                        Content="🔄 Обновить"
                        Command="{Binding RefreshCommand}"
                        Margin="0,0,8,0"/>
                
                <Button Style="{StaticResource DangerButtonStyle}"
                        Content="🗑️ Удалить"
                        Command="{Binding RemoveAutorunCommand}"
                        IsEnabled="{Binding SelectedAutorun, Converter={StaticResource NullToBoolConverter}}"
                        Margin="0,0,8,0"/>
                
                <Button Style="{StaticResource SecondaryButtonStyle}"
                        Content="⏸️ Отключить"
                        Command="{Binding DisableAutorunCommand}"
                        IsEnabled="{Binding SelectedAutorun, Converter={StaticResource NullToBoolConverter}}"
                        Margin="0,0,8,0"/>
                
                <Button Style="{StaticResource SecondaryButtonStyle}"
                        Content="📁 Папка"
                        Command="{Binding OpenFileLocationCommand}"
                        IsEnabled="{Binding SelectedAutorun, Converter={StaticResource NullToBoolConverter}}"
                        Margin="0,0,8,0"/>
                
                <Button Style="{StaticResource SecondaryButtonStyle}"
                        Content="ℹ️ Свойства"
                        Command="{Binding ShowPropertiesCommand}"
                        IsEnabled="{Binding SelectedAutorun, Converter={StaticResource NullToBoolConverter}}"/>
            </StackPanel>
        </Grid>
        
        <!-- DataGrid -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <DataGrid Style="{StaticResource ModernDataGridStyle}"
                      ItemsSource="{Binding Autoruns}"
                      SelectedItem="{Binding SelectedAutorun}"
                      ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeaderStyle}"
                      CellStyle="{StaticResource ModernDataGridCellStyle}"
                      RowStyle="{StaticResource ModernDataGridRowStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Состояние" Binding="{Binding StatusDisplay}" Width="100"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="180"/>
                    <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="200"/>
                    <DataGridTextColumn Header="Компания" Binding="{Binding Company}" Width="150"/>
                    <DataGridTextColumn Header="Источник" Binding="{Binding SourceDisplay}" Width="130"/>
                    <DataGridTextColumn Header="Подпись" Binding="{Binding SignedDisplay}" Width="100"/>
                    <DataGridTextColumn Header="Путь" Binding="{Binding Path}" Width="*"/>
                </DataGrid.Columns>
                
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Удалить" Command="{Binding RemoveAutorunCommand}"/>
                        <MenuItem Header="Отключить" Command="{Binding DisableAutorunCommand}"/>
                        <Separator/>
                        <MenuItem Header="Открыть расположение файла" Command="{Binding OpenFileLocationCommand}"/>
                        <MenuItem Header="Свойства" Command="{Binding ShowPropertiesCommand}"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
            </DataGrid>
        </Border>
        
        <!-- Status Bar -->
        <Border Grid.Row="3" 
                Background="{StaticResource SurfaceBrush}" 
                CornerRadius="6" 
                Padding="16,10" 
                Margin="0,16,0,0">
            <TextBlock Text="{Binding StatusText}" 
                       Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="12"/>
        </Border>
    </Grid>
</UserControl>

